# Changelog

按 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 记。版本号和 git tag 对得上。有改动先写在 Unreleased，打 tag 时再收进去。

## [Unreleased]

- 设备卡片内存/温度/CPU 轻量轮询从 5 秒改为 2 秒，采样缓存从 4 秒改为 1.5 秒；完整设备清单仍只在成员变化或 30 秒保底时发送，推理期间不抢流。

- 2026-09-22：修复同名聊天机被分配相同序号、中心停用后接管停滞，以及新1号可见却无法连接的问题。编号统一使用安装 UID；故障接管支持原2号也缺席的场景，只把成功完成的扫描计入确认；广播按最新观测接受2→1，监听异常重启保留已选中心；中心不再展示旧节点转发的聊天名单。此条替代下文按蓝牙名编号、仅原2号接管的旧描述。详细记录见 `docs/nearby-identity-and-hub-failover-fix-2026-09-22.md`。

- 静态审查发现的 8 个问题全部修复。最要紧的两个：聊天端的回退连接现在只拨广播编号1的算力（以前会拨到任意可信广播，连上就被对端踢掉）；聊天端的 BLE 扫描补上和算力端一致的串行化，重试轮次不再互相拆掉监听。另外广播启动失败会退避重试而不是静默失效、聊天链路断开时会说明由聊天端重连、聊天编号变化会即时改标题、状态推送不再和流式推理抢无线、广播指纹的边界判断修正。聊天端补上了缺失的通道测试。详见 `docs/code-review-findings-2026-09-21.md` 第12节。

- 中心故障接管与状态新鲜度：原2号确认1号持续缺席后升为新1号，聊天端延长有限重连窗口并修复“推理中断后不再安排重连”；状态每5秒轻量轮询、任务数变化即时推送，完整清单只在拓扑变化或30秒保底时发。详见问题记录第14节。

- 聊天机自动/手动连接后反复断开的减载修复：算力中心有聊天连接时停止 BLE 组网扫描，握手撞上扫描也立即取消；执行节点主动加入编号1；30秒才同步一次设备清单，且不再把多帧清单发给聊天机或在推理期间发送。真机证据和待验收项见 `docs/nearby-device-dedup-and-reconnect-fix-2026-09-21.md` 第13节。

- 最新中心拓扑修正：算力机经过10秒选举后发布编号，聊天端只连算力设备1；2号等只接受1号派发的执行任务，不再成为聊天协调节点。推理期间暂停 BLE 组网扫描，减少无线资源争用；短暂断链不使2号自升为第二个1号。此前条目中“连信号最强算力、广播不带编号”描述的是旧实现，现已被本条替代。详见 `docs/nearby-device-dedup-and-reconnect-fix-2026-09-21.md` 第12节。

- 算力机点「停用近场」现在会关掉广播和增强连接。以前没聊天机连着时这个按钮等于没点。
- 算力设备卡片同时显示内存占用和当前推理路数。
- 同一台设备断开再连，聊天/算力序号按蓝牙地址记住，不再按连入顺序重排。
- 聊天页标题会显示「聊天设备N」，编号由算力机在握手时告知。
- 近场只接受超级终端里已组网的同账号或可信设备；扫到别人的算力也不会连。握手时用设备名核对，不再拿蓝牙 MAC 去对超级终端的 ID（对不上就会把合法设备踢掉）。
- 算力机互相只保留一条连接，仪表盘不再把同一台显示两遍。
- 算力中心按可用内存选：余量最大的是算力设备1，然后是 2、3。平手再按蓝牙名。
- 算力机开机先广播「还没编号」，扫完旁边的算力再选 1、2、3。聊天机等到真正的算力设备1出现再连，最多等约 8 秒。
- 算力机扫完旁边的算力后还会隔几秒再扫，后开的那台也能被连上；蓝牙名比较大的那台如果对面还没连过来，会自己补一脚。
- 一张算力卡上的「推理 N」按正在跑的任务数显示。以前把本机在途和模型采样加在一起，一路任务会显示成推理 2。
- 聊天机不再弹出两个一模一样的「算力 · 算力设备1」。蓝牙地址去重后只连真正的算力设备1。
- 算力机互扫最多几次，已经组上或有聊天机连着就停，避免占着无线电导致另一台聊天机连不上。
- 算力编号一旦选出就不再退回「算力设备」。以前对端同步名单对不上蓝牙名时，标题会在「算力设备」和「算力设备1」之间跳。
- 聊天机只连算力设备1，连不上就重试，不会改去连算力设备2。先连上的聊天机号码也不再因为第二台加入而从 1 变成 2。
- 根因：编号写进 BLE 广播后每次改号都要 stopAdvertising，linkEnhance 会被拆掉（日志里是 -425918059）。现在广播只开一次，编号只在界面上显示；聊天机连信号最好的一台可信算力，推理仍由算力机 pickNode 往外派。

## [v0.3-cluster] - 2026-09-20

能看见对面有没有模型、内存多少；多台算力时由模型机选谁来跑。聊天页干净了不少。

### Added

- 近场会同步设备名、内存、已部署模型和正在跑的路数。算力机大约每 3 秒发一次 `deviceInfoSync`，被问到 `deviceInfo` 就立刻回。
- 模型端用 `pickNode` 选执行节点（`cluster/ComputeRouter.ets`）。没部署模型的不算；先挑空闲的，负载一样看可用内存，再平手用本机。本机可以多路 session，所以全忙时仍可能落到自己。集群里一台模型都没有才会返回 null。
- 派发出去的请求带 `sched=true`，对端必须自己跑，不能再转。协调机还会改写 `done.source`，免得执行机自己的「算力设备1」被原样传给聊天机。
- 两端输入栏都可以「+」选一张图，显示在气泡或任务卡上。现在这颗模型吃不了图，所以既不送推理也不走近场。有配文时发出去是「（附图）…」，没配文是「请描述这张图片。」
- 两套图标和界面重做了一遍。状态改成圆点标签，任务写成「聊天设备N  由算力设备N推理」，设备卡不再堆 MAC，内存条旁边标了「内存」。

### Changed

- 算力发现不用 128-bit UUID 了，改成 BLE manufacturer `0x6E77`、payload type `1`。
- 模型端不再写死两路上限。聊天机同时来多少路就开多少路 GEWU；CreateSession 或 start 失败才说模型繁忙。本机输入框还是一次一路。
- 本机固定叫算力设备1，其它算力按 MAC 排 2、3…，聊天设备同样按 MAC 编号。回复末尾带「由算力设备N推理」。
- 聊天页连上之后只显示「协同已就绪」，不再出现「本机 Qwen2.5-7B」。聊天机如果自己有权重会本地跑；手头这两台测试机没有。
- 源码按 `protocol` / `nearby` / `llm` / `cluster` 这些目录分开了，选点在模型端 `cluster/ComputeRouter.ets`。

### Removed

- 回复末尾「来自本机 Qwen2.5-7B-Instruct」那种尾巴。
- 两端那份 9 月 10 日的 `PACKAGE_INFO.md`（旧设备号和 SoftBus HAP 哈希）。

### Fixed

- 相册选完图后用文件描述符解码。之前直接拿 picker 的 URI 喂给 ImageKit，真机会报「图片读取失败」；点取消也不再当成失败。

选点、发现、附图这些细节 README 里有，这里不重复。两个 HAP 先保持分开；StarFlash / BLE 5 也还没上。

## [v0.2-nearfield] - 2026-09-15

数据通道从 SoftBus 的 UIAbility 协同换成 BLE `linkEnhance`。模型端改成仪表盘，聊天端还是聊天页。

### Added

- 近场消息走 `linkEnhance`。当时算力机还在用 UUID 广播，v0.3 才改成 manufacturer type。
- 打开 App 会自动发现并连接，手动「启用近场 / 连接 / 断开」也还在。
- 模型端能看到已连接设备、进行中和已完成的任务、耗时和字数，点开任务能看生成内容。
- 聊天机按接入顺序叫「聊天设备1」「聊天设备2」，当时还带完整蓝牙地址，同一地址重连序号不变。
- 推理最多两路并行（v0.3 拿掉了）。本机输入一次一路。

### Changed

- 不再拿 `abilityConnectionManager` 传聊天数据（原先一对一）。
- 模型端不再混用聊天气泡。

[Unreleased]: https://github.com/Keanu2/MiniPC/compare/v0.3-cluster...HEAD
[v0.3-cluster]: https://github.com/Keanu2/MiniPC/releases/tag/v0.3-cluster
[v0.2-nearfield]: https://github.com/Keanu2/MiniPC/releases/tag/v0.2-nearfield
